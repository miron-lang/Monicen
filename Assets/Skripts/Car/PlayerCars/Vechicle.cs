using UnityEngine;

public class Vechicle : MonoBehaviour
{

    //[Header("Whhel's Colidors")]
    //читыре переменые калайдоров калёс

    //[Header("Whel's Transforms")]
    //ситыре переменые визуала  калёс Transform

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        MoveVechicle();
    }

    void MoveVechicle()
    {

    }
}
